package secondDay4;

public interface Bank {

    // interface is a special kind of class
    // WebDriver driver = new ChromeDriver();
    
      double baseInterestRate = 10.0; // variables that can't be changed they are final/static/constant
     
      double interestRate ();// only declare the method : abstract method
}
